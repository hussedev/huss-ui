import { Meta, Title, Primary, Stories, Controls, Story } from "@storybook/blocks";

import { PoolSummary } from "./PoolSummary";
import * as PoolSummaryStories from "./PoolSummary.stories";

# PoolSummary

<Meta of={PoolSummaryStories} />

The PoolSummary component renders

This component provides a Loading and an Error State.

# Example

<Story of={PoolSummaryStories.Default} />

# Controls

<Controls />

# Other variations

<Stories />
