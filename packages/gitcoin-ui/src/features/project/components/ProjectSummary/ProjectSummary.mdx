import { Meta, Title, Primary, Stories, Controls, Story } from "@storybook/blocks";

import * as ProjectSummaryStories from "./ProjectSummary.stories";

<Meta of={ProjectSummaryStories} />

# ProjectSummary Component

The `ProjectSummary` component displays a list of project-related labels such as addresses, social media links, and dates. It uses Tailwind CSS for styling and `tailwind-variants` for variant handling.

# Example

<Story of={ProjectSummaryStories.Default} />

# Controls

<Controls />
<Stories />
