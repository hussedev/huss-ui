import { Meta, Title, Stories, Controls, Story } from "@storybook/blocks";
import * as RadioGroupListStories from "./RadioGroupList.stories";

# Radio Group List

<Meta of={RadioGroupListStories} />

The `RadioGroupList` component allows you to render multiple groups of radio buttons. It can handle the state of each selection and provides customizable action buttons for various functionalities.

## Example

<Story name="Default" of={RadioGroupListStories.Default} />

## Controls

<Controls />

## Other Variations

<Stories />
