import { Meta, Title, Stories, Controls, Story } from "@storybook/blocks";
import * as EvaluationFormStories from "./EvaluationForm.stories";

# Evaluation Form

<Meta of={EvaluationFormStories} />

The `EvaluationForm` component provides a structured way to gather user feedback through radio buttons and a text area.

## Example

<Story name="Default" of={EvaluationFormStories.Default} />

## Controls

<Controls />

## Other Variations

<Stories />
