import { Meta, Title, Primary, Stories, Controls, Story } from "@storybook/blocks";

import { Markdown } from "./Markdown";
import * as MarkdownStories from "./Markdown.stories";

# Markdown Component

<Meta of={MarkdownStories} />

The `Markdown` component allows for versatile display of styled text and supports a variety of Markdown elements. It’s especially useful for rendering dynamic content with formatting, code examples, lists, tables, and more.

## Example

### Basic Markdown Usage

<Story of={MarkdownStories.BasicMarkdown} />

## Controls

<Controls />

## Additional Variations

Explore the following variations to see how the `Markdown` component adapts to different layouts and container sizes.

<Stories />
