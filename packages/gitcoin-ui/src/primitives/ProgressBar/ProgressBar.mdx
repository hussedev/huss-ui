import { Meta, Title, Primary, Stories, Controls, Story } from "@storybook/blocks";

import { ProgressBar } from "./ProgressBar";
import * as ProgressBarStories from "./ProgressBar.stories";

# ProgressBar

<Meta of={ProgressBarStories} />

The ProgressBar shows the progress of a task.

# Example

## Default

<Story of={ProgressBarStories.Default} />

# Controls

<Controls />

# Other variations

<Stories />
